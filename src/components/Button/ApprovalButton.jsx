import { ApproveButton } from "../Common/Button.styles";

export const ApprovalButton = () => {
  return (
    <ApproveButton type="button" title="승인">
      <span>승인</span>
    </ApproveButton>
  );
};
